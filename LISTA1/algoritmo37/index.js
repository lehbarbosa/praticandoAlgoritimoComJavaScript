// Ler dois números inteiros e imprimir o produto.
const num1 = parseInt(prompt("Digite um número: "));
const num2 = parseInt(prompt("Digite um outro número: "));

const result = num1 * num2;

alert(`O produto de:  ${num1} * ${num2} = ${result}`);
